import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Product/Features/Review/Guide/Overview" />

# Review

`/report` の 3 タブ（時間の使い方 = 配分 / 差分 = 執行 / 振り返り = 質）を描く component 群。カレンダー内パネルは廃止済みで、Review は独立したフルページを持つ。Storybook では面ごとの loading / empty / populated と、狭い面（375x812 / 320px）の詰まり方を確認する。

## Story ownership

| 層       | component                                                                                                                  | Story |
| -------- | -------------------------------------------------------------------------------------------------------------------------- | ----- |
| Layout   | `ReportHeader`（粒度切替あり）/ `ReportMobileHeader`（粒度切替なし）/ `ReportGranularitySwitcher` / `ReportTabs`           | あり  |
| Chapters | `AllocationChapter` / `ExecutionChapter` + `MirrorRows` / `QualityChapter` + `CompassScatter` + `WaitingList`              | あり  |
| Sidebar  | `ReportFilterList`（デスクトップ、カレンダーと同じ骨格の 👁 一覧） / `ReportFilterDrawer`（モバイル、同じ一覧を Drawer で） | あり  |
| Detail   | `ReportDetailPanel`（slot へ portal）/ `ReportDetailSheet`（`Drawer`）                                                     | あり  |

Story を持たないのは、tRPC query か器の選択を握る配線 component（`ReportBody` / `ConnectedReportDetailPanel` / `ReportDetailBody`）。中身は上記の Story が兼ねる。

## Review が持つもの / 持たないもの

**持つ**:

- `domain/report/` — 各面の派生（分母・鏡・羅針盤・インク）はすべてここの純粋関数。閾値の定数もここ
- `server/` — `getReportPeriod`（期間集計、スカラーのみ）と `getReportActivityDetail`（詳細パネル専用）
- `stores/` — `useReportViewStore`（フィルタ。端末ローカル）/ `useReportDetailStore`（詳細の開閉）
- 章 component と、詳細の本文（`ReportDetailBody`）+ 2 つの器

**持たない**（Composition Bridge = `app/**/(workspace)/_composition/ReportViewClient.tsx` が担う）:

- ルーティング（`?tab=` の書き換えを含む）とカレンダーへのジャンプ（`useReportJump`）
- 期間ナビの配線（`useCalendarNavigation`）
- **器の選択**（デスクトップのパネル / モバイルのシート）とモバイル判定

review は同層の `features/calendar` を import できない。共有したい hook は `lib/hooks/` へ置く（`useSwipeGesture`）。

## Story を書く時の約束

- 章の Story に**評価の表現を持ち込まない**（スコア・達成率・良し悪しの色）。仕様 §0-2
- 余白は塗らない。決算バーの塗り残しがそのまま余白
- 閾値未満は数字を作らずダッシュ。`AllPatterns` にその状態を含める

分母・閾値・URL 契約・モバイルの規則は `docs/product/specs/review.md`、集計の data flow は `docs/engineering/architecture.md` を参照する。
