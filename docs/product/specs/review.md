---
status: current
last_verified: 2026-09-05
code: apps/product/src/features/review
public_docs:
  - review
lp:
  - 'Core Review metrics'
  - 'All Review metrics'
---

# Review（振り返り）

`/report` は週 / 月 / 年で切り替わる **4 章固定の読み物**。Plan と Record の距離を静かに示し、次の計画へ渡すための面。ダッシュボードではない。

## 0. 原則

台帳は評価しない。この 6 つに反する実装は、要望があっても入れない。

1. **4 章を決まった順で読む**。折りたたみ・並び替え・非表示・ウィジェットのカスタマイズを持たない
2. **評価しない**。スコア・ストリーク・カバー率・目標・達成率・推奨文・色による良し悪しを作らない
3. **数字は必ず明細に落ちる**。クリックできる数字・バー・点はすべて詳細パネル（またはカレンダー）へ着地する
4. **統計の誠実さ**。平均を出さない（中央値と `n` で語る）。閾値未満は数字を作らず沈黙する
5. **分母は時間そのもの**（週 = 168h）。書かれていない時間を「余白」と呼び、**灰色ブロックで塗らない**
6. **レポート面では作成・編集をしない**。他画面へのジャンプだけがアクション

文言は責めない・催促しない（「〜しましょう」「〜すべき」を使わない）。

## 1. 4 章

| 章  | 見出し                      | 中身                                                                                                    |
| --- | --------------------------- | ------------------------------------------------------------------------------------------------------- |
| 1   | 配分 — 何にいくら使ったか   | 決算バー（カテゴリー別）/ 前期間との差 Δ / 未分類の割合 / セグメント別バー / 日別（週別・月別）のインク |
| 2   | 執行 — 計画どおりだったか   | アクティビティ別の 記録バー + 予定バー（破線）+ 予定比 / 見積もりの鏡                                   |
| 3   | 質 — それは良い配分だったか | 羅針盤（投下時間 × 充実）と、点になるのを待っているアクティビティ                                       |
| 4   | 整える — そして来週へ       | 未分類の記録 / 未変換の外部カレンダー予定 / 次の期間の予定時間                                          |

各章のクリック先は 3 通りだけ:

- 行・点 → **詳細パネル**（デスクトップ）/ **ボトムシート**（モバイル）
- 4 章の導線 → **カレンダー**（該当の記録 / 日 / 次の期間）
- それ以外 → 無い

### 1 章の分母

`period.lengthMinutes` が期間そのものの長さ（週 = 10080 分）。

| 記号             | 実装              | 定義                                                                                |
| ---------------- | ----------------- | ----------------------------------------------------------------------------------- |
| `totalAll`       | `totalAllMinutes` | **フィルタを無視した**全アクティビティの記録合計                                    |
| `margin`（余白） | `marginMinutes`   | `lengthMinutes − totalAll`。**フィルタで動かない**                                  |
| `V`              | `visibleMinutes`  | フィルタとセグメントを通した記録合計（見えているインク）                            |
| `track`          | `trackMinutes`    | 決算バーの全長。余白 on なら `V + margin`、off なら `V`（0 除算を避けるため最小 1） |

**`computeDenominators` の `allActivities` にだけフィルタを掛けてはいけない。** 掛けるとカテゴリーを 1 つ隠しただけで余白の値が動き、「書かなかった時間」ではなく「表示の副作用」になる。カテゴリーを隠して動くのは `V` と `track` だけ。

余白はセグメントを持たない。背景トラック（紙）として残し、塗らない。

## 2. フィルタとセグメント

サイドバー（デスクトップ）とチップ列（モバイル）は**同じ `useReportViewStore`**（端末ローカル、persist）を読み書きする。

- **フィルタ**: カテゴリー / 未分類 / 余白の 3 種類。アクティビティ単位のフィルタは持たない
- 状態は `hiddenCategoryIds` で持つ（可視リストではない）。新しく作ったカテゴリーは自動で可視になる
- **セグメント**: 選ぶと 1 章の宇宙が `segment.activityIds ∩ visible` に縮む。「すべて」が既定
- セグメントはフィルタと**交差する**（フィルタを無視して全アクティビティを集計しない）
- **セグメント選択中は余白を分母に入れない**。セグメント内の記録合計が 100% になる。余白チップは押せなくする
- セグメント選択中は 1 章の凡例がカテゴリー別からアクティビティ別へ変わり、セグメント別バーはまとめて消える（選択中 100% / 他 ~0% は読み違いを生むため）
- 削除済みセグメントを指していたら「すべて」へ縮退する（正本は `useActiveSegment`）

セグメントの CRUD（作成・アクティビティ編集・削除）はデスクトップのサイドバーだけが持つ。並び替え・フォルダ分け・共有は持たない。

## 3. 閾値（沈黙の規則）

数えるに足りない回数で比率を作らない。定数は `domain/report/report-view-model.ts` にある。

| 定数                         | 値    | 効き方                                                                       |
| ---------------------------- | ----- | ---------------------------------------------------------------------------- |
| `EXECUTION_MIN_PLAN_MINUTES` | 15 分 | 2 章の予定比。過去予定がこれ未満なら `null`（0% でも空文字でもなくダッシュ） |
| `MIRROR_MIN_PLAN_MINUTES`    | 30 分 | 見積もりの鏡の候補条件                                                       |
| `MIRROR_MIN_PLAN_BOXES`      | 3 件  | 同上                                                                         |
| `COMPASS_MIN_FULFILLMENT`    | 5 件  | 羅針盤の点。充実の回答がこれ未満なら点にせず待機リストへ                     |

- **見積もりの鏡**: 「過去予定 >= 30 分」∧「記録がある」∧「過去予定の件数 >= 3」を**すべて**満たす行だけが候補。`|coef − 1|` の降順（癖の強い順）で最大 3 件。`coef = rec / planPast`。全体遵守率のような合成値は作らない
- **`planPast`** は開始が `nowAt` 以下の予定だけを数える。未来の予定で係数を汚さない
- **2 章の行は足切りしない**。記録か予定のどちらかがある行はすべて出す（決算の完全性）

## 4. 期間と URL 契約

- `/report?date=YYYY-MM-DD&range=week|month|year`
- `range` の省略・不正値・旧 `day` は **`week` へ丸める**（`parseReportRangeParam`）
- 期間の正本は `/report` 自身の `date` と `range`。カレンダーの view とは独立している
- 表示中の日付は `CalendarNavigationContext` が URL から読んで保持する。**server component の prop で受け取らない** — `navigateToDate` は `history.replaceState` を書くだけで RSC を再描画しないため、prop を正本にすると `‹ ›` を押しても画面が変わらない
- 期間の移動は必ず `useCalendarNavigation().navigateToDate(date, true)` 経由。`navigateRelative` は使わない（カレンダーの viewType 基準で動くのでレポートの粒度と食い違う）
- 週の開始は `user_settings.week_starts_on`（0 / 1 / 6。土曜始まりもある）
- 期間を移したら詳細パネルは閉じる。別の期間の明細を開いたまま残さない

## 5. 集計の分割

**server は 1 期間 1 round trip の「アクティビティ別スカラー集計」だけ返す**（`review.getReportPeriod`）。フィルタ・セグメント・分母・鏡・羅針盤の派生は client の純粋関数（`features/review/domain/report/`）が導出する。カテゴリーのトグルや余白の on/off でサーバーへ往復させない。

**タイムブロックの明細は主 payload に載せない。** 1〜4 章に要るのは件数（`plannedPastBoxes` / `recordBoxes`）と充実の回答数だけで、どれもスカラーに畳める。明細・1 件あたりの中央値・時間帯分布が要るのは詳細パネルだけなので、開いた時に `review.getReportActivityDetail` で取る。年粒度で payload が Record 件数に線形比例するのを構造的に断つ。

集計は**半開区間 + clip** で書く: `start_at < end AND end_at > start` で選び `[max(start, S), min(end, E))` へ clip する（#2426 の canonical query）。0 時またぎの記録は日別インクへ按分する。

置き場は `features/review/server/`（review が spec を所有する）。旧 `statistics-fetchers.ts` 経路は kpi / MCP が使うもので、レポートは通らない。

## 6. 詳細パネル / ボトムシート

行・点から開く。**中身は器によらず同じ**（`ReportDetailBody`）:

統計 4 枚（記録合計 / 予定との差 / 1 件あたりの中央値 / 充実の分布）→ 見積もりの鏡 → 時間帯の分布（朝・午前・昼・午後・夜・深夜の 6 バケット）→ 週別の推移 → 明細 → 「カレンダーで見る」。

- **編集はしない**（充実の後付けは編集面の仕事）
- **「（記録中）」は作らない**。Dayopt の Record は常に `end_at <= now` で、記録中という状態が存在しない
- 明細の時刻・曜日とジャンプ先の日付は、ブラウザのローカルではなく**ユーザー設定の timezone** で切る
- 器は Composition Bridge が選ぶ。**デスクトップ** = shell の 4 カラム目（250px）へ portal、**モバイル** = `Drawer`
- store（`useReportDetailStore`）は shell の 4 カラム目の開閉も握っているので、`/report` を離れる時に閉じる（中身の無い帯を他ページへ持ち越さない）

## 7. カレンダーへのジャンプ（4 章）

ルーティングは review が持たない。Composition Bridge（`_composition/useReportJump.ts`）が担い、review は props のコールバックで受ける。

| 導線                   | 飛び先                                                           |
| ---------------------- | ---------------------------------------------------------------- |
| 未分類の記録を仕分ける | 最初の未分類 Record の日を日ビューで開き、その Record を選択する |
| 未変換の外部予定を見る | 最初の未変換予定がある日                                         |
| 次の期間を開く         | 次の期間の先頭日                                                 |

件数が 0 なら導線ごと出ない。

## 8. モバイル

**計算はデスクトップと完全に同一**（§10 の 13-13）。器だけが違う。

**出さないもの**:

- 粒度切替 UI（狭い面で期間ラベルと `‹ ›` が潰れる）
- セグメントの CRUD（選ぶだけ。`束` チップの Drawer）
- 詳細シートの週別推移（`includeTrend: false` で**取得ごと**落とす。表示側だけで落とすと運ぶだけ無駄になる）

**出すもの**: 期間ラベルと `‹ ›`（`ReportMobileHeader`）、横スクロールのフィルタチップ列（`ReportFilterChipRow`）、詳細のボトムシート。

**粒度そのものは URL を尊重する。** `?range=month` をスマホで開いたら月のまま描く。デスクトップで月を見て共有したリンクが壊れないほうを選ぶ（2026-09-05 User 裁可）。§8 の「週固定」は「切替 UI を出さない」の意味に取る。

**スワイプ**で前後の期間へ動く: `|dx| > 55px ∧ |dx| > 1.4 × |dy|`。`/report` は縦スクロールが主なので、章をなぞる指で数字が黙って入れ替わらない値にしてある。移動は §4 のとおり `navigateToDate` 経由。

**面の判定の正本は shell（`BaseLayoutContent`）と同じ `MEDIA_QUERIES.mobile`**（幅のみ）。`useIsMobile()`（幅 **かつ** coarse pointer）を使ってはいけない — 狭くしたデスクトップの窓で shell は `MobileLayout`（詳細の DOM slot 無し）を描くのに中身は desktop 扱いになり、チップ列が出ないうえ行を押しても何も開かない面が生まれる。

## 9. やらないこと

実装禁止リスト（要望が来ても入れない）:

- 合成スコア・ストリーク・カバー率・目標バー・達成率
- 充実の平均値 / 5 段階化 / 色分け / 絵文字
- 羅針盤の平均・回帰線・象限の塗り分け・ランキング
- ウィジェットのカスタマイズ、日単位の分析ビュー
- 円グラフ、セグメントの合計表示（セグメント同士は重複してよい）
- レポート面での作成・編集、週の締め・ロック
- 他人との比較、通知・催促文言
- 余白を灰色セグメントで塗ること

## 10. 実装の不変条件

- **13-2**: `computeDenominators` の `allActivities` にフィルタを掛けない（§1 の分母を参照）
- **13-13**: **モバイル専用の集計を作らない**。器が違うだけで、分母もセグメントも 1 つの真実（同じ store・同じ純粋関数）に集約する
- **依存の向き**: `features/review → lib/`。review は同層の `features/calendar` を import できない。共有したい hook は `lib/hooks/` へ置く（`useSwipeGesture` がそれ）
- **query は Composition Bridge から**。tRPC query を持つ component を `ReportBody` の中へ入れない（`/report` 以外から描いた時に context を要求してしまう）
- component の visual state は Storybook（`Product/Features/Review/...`）、集計の data flow と composition は [Engineering Architecture](../../engineering/architecture.md) を参照する

## 関連する意思決定

- 2026-09-03 [product]: レポート面を 4 章構成へ全面置換（[#2575](https://github.com/Dayopt/dayopt/issues/2575)）
- 2026-09-03 [product]: 4 章の「未確認の提案」は外部カレンダーの未変換予定に読み替える（[#2580](https://github.com/Dayopt/dayopt/issues/2580)）
- 2026-09-04 [product]: Plan / Record の時刻ルールを 2 本へ（[#2598](https://github.com/Dayopt/dayopt/pull/2598)）
- ADR-025: Plan / Record モデル（削除済み、git 履歴参照）
